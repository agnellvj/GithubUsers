// import { fromJS } from 'immutable';
// import { makeSelectGithubUsersPageDomain } from '../selectors';

// const selector = makeSelectGithubUsersPageDomain();

describe('makeSelectGithubUsersPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
